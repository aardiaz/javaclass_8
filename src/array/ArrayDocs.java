package array;

public class ArrayDocs {
	
	/*
	 * --------------------- Array -------------------------------------
	 *  # collection of homogeneous data.
	 *  # collection/group of multiple values with same data type
	 *     having fixed size.
	 *     
	 *  # contains duplicate data
	 *  # data stored on the basis of indices(index starts from 0)
	 *  # random access
	 *  # contiguous memory allocation
	 *  # array is an object in java.
	 *  
	 *  # Types :
	 *   a> Single 
	 *   b> Multi-Dimensional array
	 *   
	 *  # syntax :
	 *  
	 *     data_type   array_name[]  = new  data_type[size];
	 *       
	 *                        or
	 *                   new data_type[size];
	 *                   
	 *     
	 *                   e.g.
	 *                   
	 *          float  height[] = new float[5000];
	 *          
	 *          char[]   alps = new char[26];
	 *          
	 *          String  []cname = new String[200];
	 *          
	 *          //Dog[]  dogs  = new  Dog[340];
	 */

}
